# rhds

Contains config-template.env script - copy to config.env and add file paths

Source config.env with data and results directory
Run scripts/01 to scripts/05 in order: Rscript 05-analysis.r ${datadir} ${resultsdir}
